#include <stdio.h>
 
int main()
{
   int massiv[] = {1, 2, 3, 4}; // создали массив
   massiv[0]=massiv[0]*2; // обращаемся к 0 элементу
   massiv[3]=massiv[3]*2; // обращаемся к 3 элементу
   for(int i=0; i<=3; i++) 
   {
    printf("%d\n", massiv[i]);
   }
   
    return 0;
}
